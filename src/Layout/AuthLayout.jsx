import { Outlet } from "react-router-dom";

function AuthLayout() {
  return (
    <div
      style={{
        display: "flex",
        height: "100vh",
        justifyContent: "center",
        alignItems: "center",
      }}
    ></div>
  );
}

export default AuthLayout;
